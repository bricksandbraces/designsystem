@import "../../styles/typography.pcss";
@import "../../styles/theme.pcss";

.checkbox {
  display: block;
  min-height: 1rem;
  margin-bottom: 0.25rem;
  color: $checkbox-font;

  &--label {
    @mixin type--body-small;

    display: inline-flex;
    cursor: pointer;
  }

  &--input {
    display: block;
    position: absolute;
    width: 0;
    height: 0;
    max-height: 0;
    opacity: 0;
    cursor: pointer;
  }

  &--check {
    display: inline-block;
    min-width: 20px;
    min-height: 20px;
    margin-right: $spacing-03;
    color: $warm-gray-30;

    &-mark {
      color: $white;
    }
  }
}

/* Checkmark */

.checkbox input[type="checkbox"] + label > .checkbox--check {
  & > .checkbox--check-mark {
    visibility: hidden;
  }

  & > .checkbox--check-box {
    fill: transparent;
  }
}

.checkbox input[type="checkbox"]:checked + label > .checkbox--check {
  color: $white;
  & > .checkbox--check-mark {
    visibility: visible;
    stroke-width: 2px;
    color: $black;
  }

  & > .checkbox--check-box {
    fill: $white;
  }
}

/* Disabled */

.checkbox--input[type="checkbox"]:disabled + .checkbox--label {
  color: $checkbox-disabled;
  cursor: not-allowed;

  & > .checkbox--check {
    color: $checkbox-disabled;
  }

  & * {
    color: $checkbox-disabled;
  }
}

.checkbox--input[type="checkbox"] + .checkbox--label > svg {
  transition: border-color $transition-01;
  border: 1px solid transparent;
  border-radius: 2px;
}

.checkbox--input[type="checkbox"]:focus-visible + .checkbox--label > svg {
  border-color: $focus;
}
