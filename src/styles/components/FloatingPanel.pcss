@import "../prefix.pcss";
@import "../colors.pcss";
@import "../theme.pcss";

.$(prefix)--floatingpanel {
  min-height: $container-02;
  padding: $spacing-02;
  border: 1px solid $floatingpanel-border;
  border-radius: 2px;
  background-color: $floatingpanel-background;
  color: $floatingpanel-color;
}
