.$(prefix) {
  &--sidenav {
    display: flex;
    position: sticky;
    z-index: 9000;
    top: 0;
    left: 0;
    flex-wrap: wrap;
    align-content: flex-start;
    justify-content: center;
    min-width: var(--size-container-08);
    max-width: var(--size-container-08);
    height: 100%;
    padding: 0 var(--size-spacing-inner) var(--size-spacing-inner)
      var(--size-spacing-inner);
    overflow-x: hidden;
    overflow-y: scroll;
    transition: transform var(--transition-app-01);
    border-right: var(--size-border) var(--style-border) var(--color-border);
    background-color: var(--color-background);
    gap: var(--size-spacing-02);

    &-collapsed {
      min-width: var(--size-container-05);
      max-width: var(--size-container-05);

      & .$(prefix)--sidenav-headline {
        display: none;
      }

      & .$(prefix)--sidenav-logo,
      & .$(prefix)--sidenav-logo > img,
      & .$(prefix)--sidenav-logo > svg {
        min-width: var(--size-container-04);
        max-width: var(--size-container-04);
        min-height: var(--size-container-04);
        max-height: var(--size-container-04);
      }

      & .$(prefix)--sidenav-item__label {
        display: none;
      }

      & .$(prefix)--sidenav-head {
        align-items: center;
        justify-content: center;
        padding: 0;
      }

      & .$(prefix)--sidenav-item__expander {
        display: none;
      }

      & .$(prefix)--button {
        display: none;
      }

      & .$(prefix)--sidenav-item__icon {
        margin: 0;
      }

      & .$(prefix)--sidenav-item {
        justify-content: center;
        padding: 0;
      }
    }

    &-mobileheader {
      display: none;
    }

    &-logo {
      display: inline-flex;
      align-items: center;
      transition: box-shadow var(--transition-app-01);
      border-radius: var(--component-radius-01);
      background-color: transparent;
      box-shadow: 0 0 0 0;

      & > img,
      & > svg {
        max-width: var(--size-container-07);
        max-height: var(--size-container-02);
        user-select: none;
      }

      &:focus-visible {
        outline: 0;
        box-shadow: 0 0 0 0.0625rem var(--color-focus),
          0 0 0 0.25rem var(--color-focus-highlight);
      }
    }

    &-item {
      display: inline-flex;
      position: relative;
      align-items: center;
      justify-content: flex-start;
      width: 100%;
      height: var(--size-container-04);
      padding: 0 var(--size-spacing-05);
      overflow: hidden;
      transition: background-color var(--transition-app-01),
        box-shadow var(--transition-app-01), color var(--transition-app-01);
      border: var(--size-border) var(--style-border) transparent;
      border-radius: var(--component-radius-02);
      outline: 0;
      background-color: transparent;
      box-shadow: 0 0 0 0;
      color: var(--color-font-text-03);

      &::before {
        content: "";
        position: absolute;
        left: 0;
        width: calc(var(--size-container-01) / 4);
        height: var(--size-container-02);
        border-radius: calc(var(--component-radius-01) * 2);
        opacity: 0;
        background-color: var(--color-brand-04);
      }

      &:hover {
        background-color: var(--color-hover);
        color: var(--color-font-text-01);
      }

      &:active {
        background-color: var(--color-active);
      }

      &:focus-visible {
        outline: 0;
        box-shadow: 0 0 0 0.0625rem var(--color-focus),
          0 0 0 0.25rem var(--color-focus-highlight);
      }

      &__with-icon {
        & + .$(prefix)--sidenav-item__expander-list {
          & > .$(prefix)--sidenav-item {
            padding-left: var(--size-spacing-09);
          }
        }
      }

      &__icon {
        display: flex;
        margin-right: var(--size-spacing-05);

        & > svg {
          min-width: var(--size-container-01);
          max-width: var(--size-container-01);
          min-height: var(--size-container-01);
          max-height: var(--size-container-01);
        }
      }

      &__chevron {
        & > svg {
          min-width: var(--size-container-01);
          max-width: var(--size-container-01);
          min-height: var(--size-container-01);
          max-height: var(--size-container-01);
          transition: transform var(--transition-app-01);
        }
      }

      &__label {
        @mixin type--body-02;

        white-space: nowrap;
      }

      &__container {
        display: inline-flex;
        align-items: center;
        justify-content: space-between;
        width: 100%;
      }

      &__selected {
        background-color: var(--color-selected);
        color: var(--color-font-selected);

        &::before {
          opacity: 1;
        }
      }

      &__expander {
        &-open {
          & + .$(prefix)--sidenav-item__expander-list {
            display: inline-flex;
          }

          &
            > .$(prefix)--sidenav-item__container
            > .$(prefix)--sidenav-item__chevron
            > svg {
            transform: rotate(180deg);
          }
        }

        &-list {
          display: none;
          flex-wrap: wrap;
          width: 100%;
          gap: var(--size-spacing-inner);

          & > .$(prefix)--sidenav-item {
            padding-left: calc(var(--size-spacing-05) * 2);
          }

          & + .$(prefix)--sidenav-headline {
            margin-top: var(--size-spacing-06);
          }
        }
      }

      & + .$(prefix)--sidenav-headline {
        margin-top: var(--size-spacing-05);
      }
    }

    &-divider {
      min-width: calc(100% + var(--size-spacing-inner) * 2);
      max-width: calc(100% + var(--size-spacing-inner) * 2);
      margin: calc(var(--size-spacing-05) - var(--size-spacing-inner)) 0;
    }

    &-headline {
      display: inline-flex;
      align-items: flex-end;
      width: 100%;
      margin-bottom: var(--size-spacing-03);
      padding: 0 var(--size-spacing-05);

      & > h3 {
        @mixin type--label;

        margin: 0;
        color: var(--color-font-text-04);
      }

      & > .$(prefix)--typography-label {
        margin: 0;
      }
    }

    &-head {
      display: inline-flex;
      position: sticky;
      z-index: 9000;
      top: 0;
      align-items: center;
      justify-content: space-between;
      min-width: calc(100% + var(--size-spacing-inner) * 2);
      max-width: calc(100% + var(--size-spacing-inner) * 2);
      height: calc(var(--size-container-06) + (var(--size-container-01) / 2));
      margin-bottom: var(--size-spacing-05);
      padding: 0 calc(var(--size-spacing-05) + var(--size-spacing-inner));
      background-color: var(--color-background);

      &::after {
        content: "";
        position: absolute;
        bottom: calc(var(--size-spacing-05) * -1 + 0.0625rem);
        left: 0;
        width: 100%;
        height: var(--size-container-01);
        background: linear-gradient(
          to top,
          var(--color-background-alpha-0),
          var(--color-background)
        );
      }

      & > .$(prefix)--toggle {
        margin: 0;

        & > .$(prefix)--toggle-label > .$(prefix)--toggle-switch {
          margin: 0;
        }
      }
    }
  }
}

@media screen and (max-width: $breakpoint-md) {
  .$(prefix) {
    &--sidenav {
      position: fixed;
      transform: translateX(calc(var(--size-container-08) * -1));

      &-mobileheader {
        display: flex;
        position: sticky;
        z-index: 9100;
        top: 0;
        left: 0;
        align-items: center;
        width: 100%;
        height: var(--size-container-05);
        padding: 0 var(--size-spacing-03);
        border-bottom: var(--size-border) var(--style-border)
          var(--color-border);
        background-color: var(--color-background);
      }

      &-overlay {
        visibility: hidden;
        position: fixed;
        z-index: 8900;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        transition: visibility var(--transition-app-01),
          opacity var(--transition-app-01);
        opacity: 0;
        background-color: var(--color-overlay);
      }

      &-open {
        transform: unset;
        box-shadow: var(--component-shadow-02);

        & + .$(prefix)--sidenav-overlay {
          visibility: visible;
          opacity: 1;
        }
      }
    }
  }

  .$(prefix)--sidenav-mobileheader ~ .$(prefix)--sidenav {
    top: var(--size-container-05);
  }
}

@media screen and (min-width: $breakpoint-lg) {
  .$(prefix)--sidenav-from-header {
    display: none;
  }
}
