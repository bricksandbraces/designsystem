@import "../prefix.pcss";
@import "../theme.pcss";

.$(prefix)--date-picker {
  position: absolute;
  border: $border $focus;
  background: black;
  &__hidden {
    visibility: hidden;
    opacity: 0;
  }
}
